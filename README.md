# sweet-cd
This tool is a lightweight, customizable Continuous Deployment (CD) system written in Go.
It uses a dynamic configuration (YAML/JSON) for managing services, endpoints, API tokens, and command execution.
